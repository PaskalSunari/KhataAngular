import { DeptStockLocationMapping } from './dept-stock-location-mapping.model';

describe('DeptStockLocationMapping', () => {
  it('should create an instance', () => {
    expect(new DeptStockLocationMapping()).toBeTruthy();
  });
});
